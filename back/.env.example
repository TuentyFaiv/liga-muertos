# Server Configuration
# Railway automatically sets PORT environment variable
# For local development, defaults to 8080
PORT=4000

# SurrealDB Cloud Configuration
SURREAL_URL=ws://your-surrealdb-cloud-endpoint:8000/rpc
SURREAL_NAMESPACE=your_namespace
SURREAL_DATABASE=your_database
SURREAL_USER=your_username
SURREAL_PASS=your_password

# Clerk Authentication
CLERK_SECRET_KEY=your_clerk_secret_key
CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key

# Railway Deployment Notes:
# - PORT is automatically provided by Railway
# - Set all other variables in Railway dashboard
# - Use Railway's built-in secrets management for sensitive data

# Optional: Local SurrealDB (for development)
# SURREAL_URL=ws://127.0.0.1:8000
# SURREAL_USER=root
# SURREAL_PASS=root

# Enhanced Logging Configuration
# RUST_LOG controls overall log filtering (supports module-specific levels)
# Examples:
# RUST_LOG=debug                                    # Debug level for all modules
# RUST_LOG=info,liga_muertos_back=debug            # Info globally, debug for our app
# RUST_LOG=liga_muertos_back::routes=debug         # Debug only for routes module
RUST_LOG=liga_muertos_back=info,actix_web=info

# LOG_LEVEL provides a simple way to set overall verbosity
# Options: trace, debug, info, warn, error, off
LOG_LEVEL=info

# RUST_LOG_STYLE controls colored output
# Options: auto, always, never
RUST_LOG_STYLE=auto
